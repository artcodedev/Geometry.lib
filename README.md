# geometry.lib

Установка зависимостей:
```bash
bun install
```

Пример запуcка результата:
```bash
bun index.ts
```

bunx tsc --noEmit пройден

This project was created using `bun init` in bun v1.3.4. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
